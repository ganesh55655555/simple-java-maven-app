pipeline{
    agent any
    environment{
        PATH = "/opt/maven/bin:$PATH"
        }
    stages{
        stage('build'){
            sh "mvn clean install"
        }
        stage('sonarqube analysis'){
            withsonarqybeEnv(sonarqube-server){
                sh "${scannerhome}/bin/sonar-scanner"
            }
            }
        }
        
    }
